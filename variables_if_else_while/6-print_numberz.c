#include <stdio.h>

/**
 * main - checkea si un numero es positivo, negativo o 0
 * Return: return 0
 */

int main(void)
{
	int num = 0;

	for (num = 0; num < 10; num++)
		putchar(num + '0');

	putchar('\n');
	return (0);
}
